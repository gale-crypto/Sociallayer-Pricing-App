VITE_PADDLE_CLIENT_TOKEN=your_paddle_client_token
VITE_PADDLE_ENVIRONMENT=sandbox
# Optional: Chrome extension ID so the pricing app can notify the extension after payment (unlock Pro).
# Get it from chrome://extensions (Developer mode → ID under the extension), or use the Web Store ID when published.
# VITE_EXTENSION_ID=abcdefghijklmnopqrstuvwxyzabcdef
